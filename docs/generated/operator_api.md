# Operator API endpoints (generated)

Generated by `scripts/gen_docs.py` from the FastAPI app — do not edit by hand. Narrative, examples and the SSE format: [../api_reference.md](../api_reference.md). Live docs: http://127.0.0.1:8770/docs .

| method | path | summary | parameters / body |
|---|---|---|---|
| GET | `/api/v1/artifacts/search` | Past accepted answers that resemble `q` (operator knowledge base): brief, confidence, tools, whether a full report exists. | `q` (query), `category`? (query), `k`? (query) |
| POST | `/api/v1/cascade` | Passenger cascade simulator: which open stations receive the closed stations' passengers and how far above their typical load they go (`overflow_ratio`, `at_risk` ≥ 1.3×). An assumption scenario (stated in `method` / `assumption`), not a measurement. | body **CascadeBody**: `closed_stations`, `timestamp`, `share`, `hops` |
| GET | `/api/v1/centrality` | The `n` busiest stations with daily flow and betweenness centrality. | `n`? (query) |
| POST | `/api/v1/chat` | Ask the agent. Returns the answer (Markdown) plus everything the desktop shows next to it: `turn_id` / `artifact_turn_id` / `requires_action` / `precedent` (for feedback), `steps` (operations log), `tools`, `llm`, `tokens` {in,out,total,estimated}, `timing` {supervisor_s (Dispatcher), worker_evaluator_s (Analyst + Inspector), mcp_s, writer_s, total_s}, `counts`. | body **ChatBody**: `message`, `operator_id`, `session_id`, `context_mode`, `link_turn_id` |
| POST | `/api/v1/chat/stream` | Same as `/chat`, streamed as Server-Sent Events so the UI can show the operations while the agent works. Events (`event:` name → JSON `data:`): `choice` (the topic-switch question, then the stream ends) · `step` {kind: route/tool/worker/evaluator/llm/writer, stage: dispatcher/analyst/inspector/writer, label, seconds, detail} · `answer` (the complete response, identical to `/chat`) · `error` {message}. | body **ChatBody**: `message`, `operator_id`, `session_id`, `context_mode`, `link_turn_id` |
| GET | `/api/v1/closures` | Recorded closures (training + test): {id, when, end, duration_hours, description, closure_type: line_suspension/station_closure, line, segment, reason}. | — |
| GET | `/api/v1/conversations` | The chat history: one entry per conversation (a conversation resumed from the history is still one entry), newest first — `title` = the first question, `category`, `n_turns`, `started`, `last` (epoch seconds), `mean_score`, `resumed`, `session_ids`, `last_session_id`, `last_turn_id`. | `operator_id`? (query), `limit`? (query) |
| GET | `/api/v1/conversations/{session_id}` | All questions and answers of a conversation (and of the sessions it was resumed in), oldest first, with what the UI needs to show rating / action controls again (`turn_id`, `has_artifact`, `requires_action`, `operator_score`, `action_reported`), and `resume_turn_id`: send it as `link_turn_id` (no session_id) to continue the conversation. | `session_id` (path) |
| GET | `/api/v1/energy` | Energy per passenger by line (Wh per passenger, worst first) with the evidence that explains it: {ranking:[{line, wh_per_pax, mwh_day, pax_day, n_stations, pax_per_station_day, corr_energy_pax, weekend_vs_weekday_*_pct, efficiency_rank, explanation}], unit, method, limits}. Same method as the agent's energy tool. | — |
| GET | `/api/v1/enums` | The allowed values, for building the form (labels are the UI's business). | — |
| POST | `/api/v1/feedback` | Score and / or action in one call. Feeds the knowledge base, the knowledge graph and (if configured) the memory agent. Returns the record and what it was fed to (`fed`). | body **FeedbackBody**: `turn_id`, `operator_id`, `score`, `action_text`, `followed`, `outcome`, `occurred_at`, `comment` |
| GET | `/api/v1/feedback/pending` | Answers that asked for an action and still have no action report: the list behind a 'you have N situations to close' badge. | `operator_id`? (query), `limit`? (query) |
| GET | `/api/v1/feedback/stats` | Number and mean of scores, histogram, how often advice was followed, outcomes, by category, action-report rate. | `operator_id`? (query) |
| PATCH | `/api/v1/feedback/{feedback_id}` | Edit a record — typically add the `outcome` once it is known ("it worked" / "did_not_work"), or correct the action text. The graph is refreshed. | `feedback_id` (path); body **PatchBody**: `score`, `comment`, `action_text`, `followed`, `outcome`, `occurred_at` |
| DELETE | `/api/v1/feedback/{feedback_id}` | Withdraw a record (it no longer counts in scores or precedents). | `feedback_id` (path) |
| GET | `/api/v1/flows/daily` | Network passengers per day (training + test window) with a 7-day rolling mean: {dates[], values[], rolling_mean[]}. | — |
| GET | `/api/v1/flows/heatmap` | Mean passengers per hour per station: all stations of ONE line, else the 20 busiest of the selected lines (`lines=U1,U2`): {stations[], station_ids[], values[][24], max_value, count, lines}. | `lines`? (query) |
| GET | `/api/v1/flows/hourly-profile` | Network passengers per hour of the day, mean over weekdays and over weekends: [{hour: 'HH:00', weekday, weekend}]. | — |
| GET | `/api/v1/flows/station/{name}` | One station: hourly weekday / weekend profile, peak hours, daily totals. | `name` (path) |
| GET | `/api/v1/graph/audit` | Wrong or uncategorised branches: problems without situation / category / kind, follow-up-shaped problems, untyped actions, orphan feedback. | — |
| POST | `/api/v1/graph/repair` | Repair what the audit finds (nothing is deleted): kinds, situations, categories with domains, follow-up marking, action types. Returns before / fixed / after. | — |
| GET | `/api/v1/graph/stats` | Size of the knowledge graph by node / relationship type (Feedback, OperatorAction, Artifact … included). | — |
| GET | `/api/v1/graph/taxonomy` | The graph by category and domain (Events, Disruptions, Stations, Network, Energy, Diagnostics, Operations planning, Strategy): situations per category, and how often operators used each ActionType (staff_deployment, replacement_bus, passenger_information, rerouting, crowd_control, monitoring, service_change, coordination, other). | — |
| GET | `/api/v1/health` | Is the API up, and how much is stored. | — |
| GET | `/api/v1/network/edges` | Track connections, one row per (edge, serving line): {line, from, to, from_lat, from_lon, to_lat, to_lon}. | — |
| GET | `/api/v1/operator-actions` | Every action operators reported (newest first), filtered by category / a station in the text / minimum score. | `category`? (query), `station`? (query), `min_score`? (query), `limit`? (query) |
| GET | `/api/v1/ops/series` | Network passengers per 15 minutes for one day (`YYYY-MM-DD`) with the typical value — the sparkline under the map. | `date` (query) |
| GET | `/api/v1/ops/snapshot` | The network at one 15-minute slot (ISO time): passengers per station vs the typical value for that weekday and slot, line loads, network total, busiest stations, closures active then (with the blocked edges and unserved stations), events around then, weather, and alerts. | `at` (query) |
| GET | `/api/v1/ops/timeline` | The replay window (start, end, 15-minute step), a default time with an active closure, and all closures (for markers on the time bar). There is no live feed: the desktop replays recorded data. | — |
| GET | `/api/v1/ops/topology` | Stations (id, short name, lon/lat, lines), edges (with the lines that serve them) and line colours — enough to draw the network map. | — |
| GET | `/api/v1/precedents` | What operators did in situations similar to `question` and how it went, plus the ready-made one-line `precedent_line` the brief would show. | `question` (query), `category`? (query), `k`? (query) |
| GET | `/api/v1/stations` | The 167 stations: {name (exact), short_name, lat, lon, lines[], daily_flow (mean passengers per day), betweenness, degree}. | — |
| GET | `/api/v1/status` | Liveness of the data layer and of the agent: {status, stations_count, flows_rows, data_window, lines, agent_up, models{role: model}, quality_db, knowledge_base}. | — |
| GET | `/api/v1/turns` | Recent answers of an operator / session with `operator_score`, `n_feedback`, `requires_action`, `action_reported`, `has_report`, the brief-less summary fields the UI needs to show 'rate this answer' and 'tell us what you did'. `needs_action_report=true` = only accepted answers that asked for an action and have no action report yet. | `operator_id`? (query), `session_id`? (query), `limit`? (query), `needs_action_report`? (query) |
| GET | `/api/v1/turns/{turn_id}` | One answer: question, the answer text as stored, confidence, recommended actions, precedent line, tools used, and all its feedback records. | `turn_id` (path) |
| POST | `/api/v1/turns/{turn_id}/action` | Report what was done about the situation (the field under an answer: 'what did you do?'). Stations and lines named in the text are extracted and linked in the graph; the action is matched against the actions the answer recommended. It becomes a precedent for similar situations. | `turn_id` (path); body **ActionBody**: `operator_id`, `action_text`, `followed`, `outcome`, `occurred_at`, `score`, `comment` |
| GET | `/api/v1/turns/{turn_id}/artifact` | The whole stored bundle of an answer (plan, every tool call with arguments / time / result preview, facts, confidence + reasons, checks, LLM calls, brief, report, precedents). | `turn_id` (path) |
| GET | `/api/v1/turns/{turn_id}/feedback` | All feedback records of one answer. | `turn_id` (path) |
| GET | `/api/v1/turns/{turn_id}/report` | The full report of an answer IF it was generated before (the operator asked why / evidence / tools in chat). It is not generated here: the UI sends "why?" to the agent (same session) — then the report is stored and this endpoint returns it. The deterministic 'how this was worked out' section is always available as `method`. | `turn_id` (path) |
| POST | `/api/v1/turns/{turn_id}/score` | Rate a response (1-5). A response rated 1-2 is no longer reused from history. | `turn_id` (path); body **ScoreBody**: `operator_id`, `score`, `comment` |
