# Secrets — never commit (API keys live here)
.env
.env.*
!.env.example

# Python environments and caches
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/

# Regenerable build/runtime artefacts
# parquet/pickle caches, rebuilt automatically (17 MB)
ml/cache/
# ADK dev-UI session database
agent/.adk/
# observability database: every question asked, answers, traces, eval results
observability/*.db*
# evaluation reports and the filled-in copy of the answer workbook
evaluation/output/
.streamlit/secrets.toml

# OS / editor
.DS_Store
.idea/
.vscode/
# experiment run outputs (regenerable from observability/agent_obs.db)
experiments/output/
observability/backup/

.run/

# React operator desktop
frontend/node_modules/
frontend/dist/
